{
  "version": 2,
  "installCommand": "npm ci && npm run build",
  "builds": [
    { "src": "dist/index.js", "use": "@vercel/node", "config": { "maxDuration": 30 } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/index.js" }
  ]
}